# Frontend dependencies. The backend needs AniVerseApiUrl/requirements.txt as
# well -- installing only this file leaves it unable to import Crypto, numpy,
# selectolax and the rest.
fastapi>=0.139.0
uvicorn>=0.51.0
jinja2>=3.1.6
httpx>=0.28.1
python-multipart>=0.0.32
